{
  "name": "database-backup-manager",
  "version": "0.1.0",
  "private": true,
  "packageManager": "pnpm@11.1.1",
  "scripts": {
    "web:dev": "pnpm -C apps/web dev",
    "web:build": "pnpm -C apps/web build",
    "web:test": "pnpm -C apps/web test",
    "e2e": "pnpm -C apps/e2e test",
    "e2e:install": "pnpm -C apps/e2e exec playwright install chromium"
  }
}
