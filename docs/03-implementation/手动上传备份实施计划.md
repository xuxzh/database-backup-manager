# 手动上传备份实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**目标：** 支持数据源服务器无法被管理台直接访问时，由用户上传已有数据库备份包，并由管理台上传到已配置的备份服务器。

**架构：** 新增手动上传执行链路，复用现有备份目标适配器、远端验证、运行历史、日志、下载和删除能力。新备份路径统一增加类型前缀：自动备份写入 `scheduled/`，手动上传写入 `manual/`；旧历史和旧远端文件不迁移。

**技术栈：** Rust、Axum multipart、SQLx SQLite migration、Tokio 文件 IO、现有 SSH/rsync target adapter、React、Vite、TypeScript。

---

## 范围和约定

- 不迁移旧历史数据。
- 不迁移旧远端备份文件。
- 新产生的自动备份远端路径从 `~/backups/<source>/<type>/<db>/<date>/...` 调整为 `~/backups/scheduled/<source>/<type>/<db>/<date>/...`。
- 新产生的手动上传远端路径为 `~/backups/manual/<source>/<type>/<db>/<date>/...`。
- 手动上传不执行数据库导出，不执行压缩，只保存用户上传的文件并上传到备份目标。
- 手动上传第一版不做恢复校验。

## 文件结构

- 修改 `crates/backup-manager/Cargo.toml`：启用 Axum multipart。
- 修改 `crates/backup-manager/src/config.rs`：新增手动上传大小限制和扩展名配置。
- 修改 `crates/backup-manager/src/domain.rs`：新增 run 类型、手动上传 DTO、手动上传元数据模型。
- 修改 `crates/backup-manager/src/repository.rs`：新增创建手动上传 run、保存/查询手动上传元数据、读取 run 类型。
- 新增 `crates/backup-manager/migrations/202605230002_manual_backup_uploads.sql`：扩展运行记录并创建手动上传表。
- 新增 `crates/backup-manager/src/manual_upload.rs`：实现接收文件后的校验、落盘、checksum、manifest、上传、远端验证和 run 状态更新。
- 修改 `crates/backup-manager/src/main.rs`：注册 `manual_upload` 模块并注入执行器。
- 修改 `crates/backup-manager/src/api.rs`：注册 `/api/manual-uploads`，接收 multipart，返回 `BackupRun`。
- 修改 `crates/backup-manager/src/executor.rs`：自动备份远端路径增加 `scheduled/` 前缀。
- 修改 `apps/web/src/types/api.ts`：新增 run type 和手动上传类型。
- 新增 `apps/web/src/shared/api/manualUploads.ts`：封装 multipart 上传。
- 新增 `apps/web/src/features/manual-uploads/ManualUploadsPanel.tsx`：手动上传页面。
- 修改 `apps/web/src/App.tsx`：增加 `/manual-uploads` 路由、上传处理和轮询。
- 修改 `apps/web/src/app/navigation.tsx`：增加“手动上传”导航。
- 修改 `apps/web/src/features/runs/RunsPanel.tsx`：展示自动/手动类型，支持按类型筛选，手动 run 分组显示为“手动上传”。
- 修改 `apps/web/src/shared/formatters/run.ts`：新增手动上传阶段文案。
- 更新 `docs/02-architecture/数据库备份管理台架构设计.md` 或 `docs/06-operations/部署快速开始.md`：说明手动上传流程和远端路径规则。

---

## Task 1: 后端依赖和配置

**Files:**
- Modify: `crates/backup-manager/Cargo.toml`
- Modify: `crates/backup-manager/src/config.rs`

- [ ] **Step 1: 启用 Axum multipart**

将 Axum 依赖改为：

```toml
axum = { version = "0.8", features = ["macros", "multipart"] }
```

- [ ] **Step 2: 增加上传配置**

在 `AppConfig` 中增加字段：

```rust
pub max_manual_upload_bytes: u64,
pub manual_upload_allowed_extensions: Vec<String>,
```

在 `AppConfig::from_env()` 中读取：

```rust
let max_manual_upload_bytes = env_u64("MAX_MANUAL_UPLOAD_BYTES", 2 * 1024 * 1024 * 1024)?;
let manual_upload_allowed_extensions = std::env::var("MANUAL_UPLOAD_ALLOWED_EXTENSIONS")
    .unwrap_or_else(|_| "gz,zip,tar.gz,sql.gz,dump,bak,bacpac".to_string())
    .split(',')
    .map(|value| value.trim().to_ascii_lowercase())
    .filter(|value| !value.is_empty())
    .collect();
```

如果当前没有 `env_u64` helper，新增：

```rust
fn env_u64(key: &str, default: u64) -> anyhow::Result<u64> {
    match std::env::var(key) {
        Ok(value) if !value.trim().is_empty() => Ok(value.trim().parse()?),
        _ => Ok(default),
    }
}
```

- [ ] **Step 3: 运行后端快速检查**

Run:

```bash
cargo fmt
cargo test config
```

Expected: PASS。

---

## Task 2: 数据库 migration 和领域模型

**Files:**
- Create: `crates/backup-manager/migrations/202605230002_manual_backup_uploads.sql`
- Modify: `crates/backup-manager/src/domain.rs`
- Modify: `crates/backup-manager/src/repository.rs`

- [ ] **Step 1: 新增 migration**

创建 migration：

```sql
ALTER TABLE backup_runs ADD COLUMN run_type TEXT NOT NULL DEFAULT 'scheduled';

CREATE TABLE IF NOT EXISTS manual_backup_uploads (
  id TEXT PRIMARY KEY,
  backup_run_id TEXT NOT NULL,
  backup_target_id TEXT NOT NULL,
  source_label TEXT NOT NULL,
  database_type TEXT NOT NULL,
  database_name TEXT NOT NULL,
  original_file_name TEXT NOT NULL,
  note TEXT,
  created_at TEXT NOT NULL,
  FOREIGN KEY(backup_run_id) REFERENCES backup_runs(id),
  FOREIGN KEY(backup_target_id) REFERENCES backup_targets(id)
);
```

- [ ] **Step 2: 扩展 `BackupRun`**

在 `BackupRun` 中增加：

```rust
pub run_type: String,
```

现有自动 run 创建时写入：

```rust
run_type: "scheduled".to_string(),
```

- [ ] **Step 3: 新增手动上传类型**

在 `domain.rs` 中增加：

```rust
#[derive(Debug, Clone, Serialize, Deserialize)]
#[serde(rename_all = "camelCase")]
pub struct ManualBackupUpload {
    pub id: String,
    pub backup_run_id: String,
    pub backup_target_id: String,
    pub source_label: String,
    pub database_type: String,
    pub database_name: String,
    pub original_file_name: String,
    pub note: Option<String>,
    pub created_at: DateTime<Utc>,
}

#[derive(Debug, Clone)]
pub struct CreateManualBackupUpload {
    pub backup_target_id: String,
    pub source_label: String,
    pub database_type: String,
    pub database_name: String,
    pub original_file_name: String,
    pub note: Option<String>,
}
```

- [ ] **Step 4: 新增 repository 方法**

新增：

```rust
pub async fn create_manual_run(
    &self,
    input: CreateManualBackupUpload,
) -> anyhow::Result<(BackupRun, ManualBackupUpload)> {
    let now = Utc::now();
    let run = BackupRun {
        id: Uuid::new_v4().to_string(),
        backup_job_id: String::new(),
        run_type: "manualUpload".to_string(),
        status: RunStatus::Pending,
        stage: "prepare".to_string(),
        started_at: now,
        finished_at: None,
        duration_ms: None,
        raw_file_name: None,
        archive_file_name: None,
        file_size: None,
        checksum: None,
        remote_path: None,
        file_deleted: false,
        file_deleted_at: None,
        error_message: None,
        created_at: now,
    };

    let upload = ManualBackupUpload {
        id: Uuid::new_v4().to_string(),
        backup_run_id: run.id.clone(),
        backup_target_id: input.backup_target_id,
        source_label: input.source_label,
        database_type: input.database_type,
        database_name: input.database_name,
        original_file_name: input.original_file_name,
        note: input.note,
        created_at: now,
    };

    let mut tx = self.pool.begin().await?;
    sqlx::query(
        "INSERT INTO backup_runs (id, backup_job_id, run_type, status, stage, started_at, created_at)
         VALUES (?, ?, ?, ?, ?, ?, ?)",
    )
    .bind(&run.id)
    .bind(&run.backup_job_id)
    .bind(&run.run_type)
    .bind(run.status.to_string())
    .bind(&run.stage)
    .bind(run.started_at.to_rfc3339())
    .bind(run.created_at.to_rfc3339())
    .execute(&mut *tx)
    .await?;

    sqlx::query(
        "INSERT INTO manual_backup_uploads (id, backup_run_id, backup_target_id, source_label, database_type, database_name, original_file_name, note, created_at)
         VALUES (?, ?, ?, ?, ?, ?, ?, ?, ?)",
    )
    .bind(&upload.id)
    .bind(&upload.backup_run_id)
    .bind(&upload.backup_target_id)
    .bind(&upload.source_label)
    .bind(&upload.database_type)
    .bind(&upload.database_name)
    .bind(&upload.original_file_name)
    .bind(&upload.note)
    .bind(upload.created_at.to_rfc3339())
    .execute(&mut *tx)
    .await?;

    tx.commit().await?;
    Ok((run, upload))
}
```

- [ ] **Step 5: 更新 row mapper**

`row_backup_run` 读取：

```rust
run_type: row.try_get("run_type")?,
```

- [ ] **Step 6: 运行 repository 测试**

Run:

```bash
cargo test repository
```

Expected: PASS。

---

## Task 3: 手动上传执行器

**Files:**
- Create: `crates/backup-manager/src/manual_upload.rs`
- Modify: `crates/backup-manager/src/main.rs`

- [ ] **Step 1: 创建执行器结构**

新增：

```rust
use std::{path::PathBuf, sync::Arc};

use anyhow::{anyhow, bail, Context};
use chrono::Utc;
use serde_json::json;
use sha2::{Digest, Sha256};
use tokio::{fs, io::AsyncWriteExt};

use crate::{
    adapters::target::{TargetRegistry, UploadRequest},
    config::AppConfig,
    crypto::Crypto,
    domain::{BackupRun, CreateManualBackupUpload, ManualBackupUpload, RunStatus},
    repository::Repository,
};

#[derive(Clone)]
pub struct ManualUploadExecutor {
    config: Arc<AppConfig>,
    repository: Arc<Repository>,
    target_registry: Arc<TargetRegistry>,
    crypto: Arc<Crypto>,
    backups_dir: PathBuf,
}
```

- [ ] **Step 2: 实现入口方法**

```rust
impl ManualUploadExecutor {
    pub fn new(
        config: Arc<AppConfig>,
        repository: Arc<Repository>,
        target_registry: Arc<TargetRegistry>,
        crypto: Arc<Crypto>,
        backups_dir: PathBuf,
    ) -> Self {
        Self {
            config,
            repository,
            target_registry,
            crypto,
            backups_dir,
        }
    }

    pub async fn enqueue(
        &self,
        input: CreateManualBackupUpload,
        file_bytes: Vec<u8>,
    ) -> anyhow::Result<BackupRun> {
        self.validate_file(&input.original_file_name, file_bytes.len() as u64)?;
        let (run, upload) = self.repository.create_manual_run(input).await?;
        let executor = self.clone();
        let run_clone = run.clone();
        tokio::spawn(async move {
            if let Err(error) = executor.execute(run_clone, upload, file_bytes).await {
                tracing::error!(%error, "manual upload failed");
            }
        });
        Ok(run)
    }
}
```

- [ ] **Step 3: 实现文件校验**

```rust
fn validate_file(&self, file_name: &str, size: u64) -> anyhow::Result<()> {
    if size == 0 {
        bail!("uploaded file is empty");
    }
    if size > self.config.max_manual_upload_bytes {
        bail!("uploaded file exceeds max_manual_upload_bytes");
    }
    let lower = file_name.to_ascii_lowercase();
    let allowed = self
        .config
        .manual_upload_allowed_extensions
        .iter()
        .any(|ext| lower.ends_with(&format!(".{ext}")) || lower == *ext);
    if !allowed {
        bail!("uploaded file extension is not allowed");
    }
    Ok(())
}
```

- [ ] **Step 4: 实现执行步骤**

执行器阶段：

```rust
prepare -> receive_file -> checksum -> manifest -> upload -> verify_remote -> done
```

核心逻辑：

```rust
async fn execute(
    &self,
    run: BackupRun,
    upload: ManualBackupUpload,
    file_bytes: Vec<u8>,
) -> anyhow::Result<()> {
    let started_at = run.started_at;
    if let Err(error) = self.execute_inner(&run, &upload, file_bytes).await {
        let message = format!("{error:#}");
        let stage = message
            .split_whitespace()
            .find_map(|part| part.strip_prefix("stage="))
            .unwrap_or("failed")
            .to_string();
        self.repository.add_run_log(&run.id, "ERROR", &stage, &message).await?;
        self.repository.finish_run_failed(&run.id, started_at, &stage, message).await?;
    }
    Ok(())
}
```

- [ ] **Step 5: 上传远端路径使用 `manual/` 前缀**

远端目录：

```rust
let remote_dir = format!(
    "manual/{}/{}/{}/{}",
    path_slug(&upload.source_label),
    path_slug(&upload.database_type),
    path_slug(&upload.database_name),
    Utc::now().format("%Y-%m-%d")
);
```

如果 `executor.rs` 中的 `path_slug` 是私有函数，提取到新文件 `crates/backup-manager/src/path_utils.rs`，并让自动备份和手动上传共同使用。

- [ ] **Step 6: 注册模块和 AppState**

在 `main.rs` 增加：

```rust
mod manual_upload;
mod path_utils;
```

创建：

```rust
let manual_upload_executor = Arc::new(ManualUploadExecutor::new(
    config.clone(),
    repository.clone(),
    target_registry.clone(),
    crypto.clone(),
    config.backups_dir.clone(),
));
```

并注入 `AppState`。

---

## Task 4: 自动备份路径增加 `scheduled/`

**Files:**
- Modify: `crates/backup-manager/src/executor.rs`
- Create or Modify: `crates/backup-manager/src/path_utils.rs`

- [ ] **Step 1: 提取 `path_slug`**

新增：

```rust
pub fn path_slug(value: &str) -> String {
    let mut output = String::new();
    let mut pending_separator = false;

    for ch in value.trim().chars() {
        if ch.is_ascii_alphanumeric() || ch == '_' {
            if pending_separator && !output.is_empty() {
                output.push('-');
            }
            output.push(ch);
            pending_separator = false;
        } else {
            pending_separator = !output.is_empty();
        }
    }

    if output.is_empty() {
        "unknown".to_string()
    } else {
        output
    }
}
```

- [ ] **Step 2: 修改自动备份远端目录**

将自动备份的 `remote_dir` 改为：

```rust
let remote_dir = format!(
    "scheduled/{}/{}/{}/{}",
    path_slug(&source.host),
    path_slug(&source.db_type),
    database_slug,
    backup_date
);
```

- [ ] **Step 3: 更新路径相关测试**

如果现有测试断言远端路径，更新预期为包含 `scheduled/`。

Run:

```bash
cargo test executor
```

Expected: PASS。

---

## Task 5: 手动上传 API

**Files:**
- Modify: `crates/backup-manager/src/api.rs`

- [ ] **Step 1: 扩展 AppState**

增加：

```rust
pub manual_upload_executor: Arc<ManualUploadExecutor>,
```

- [ ] **Step 2: 注册路由**

```rust
.route("/manual-uploads", post(create_manual_upload))
```

- [ ] **Step 3: 实现 multipart handler**

使用：

```rust
use axum::extract::Multipart;
```

handler 签名：

```rust
async fn create_manual_upload(
    _auth: Authenticated,
    State(state): State<AppState>,
    mut multipart: Multipart,
) -> Result<Json<BackupRun>, ApiError>
```

解析字段：

```rust
let mut backup_target_id = String::new();
let mut source_label = String::new();
let mut database_type = String::new();
let mut database_name = String::new();
let mut note = None;
let mut file_name = None;
let mut file_bytes = None;
```

文件字段使用：

```rust
let name = field.name().unwrap_or_default().to_string();
if name == "file" {
    file_name = field.file_name().map(|value| value.to_string());
    file_bytes = Some(field.bytes().await?.to_vec());
}
```

- [ ] **Step 4: 字段校验**

缺少字段时返回中文错误：

```rust
if backup_target_id.trim().is_empty() {
    return Err(ApiError::bad_request("请选择备份目标"));
}
if source_label.trim().is_empty() {
    return Err(ApiError::bad_request("请输入来源名称"));
}
if database_type.trim().is_empty() {
    return Err(ApiError::bad_request("请选择数据库类型"));
}
if database_name.trim().is_empty() {
    return Err(ApiError::bad_request("请输入数据库名"));
}
```

并先检查目标存在：

```rust
state.repository.get_backup_target(&backup_target_id).await?;
```

- [ ] **Step 5: 调用执行器**

```rust
let run = state
    .manual_upload_executor
    .enqueue(
        CreateManualBackupUpload {
            backup_target_id,
            source_label,
            database_type,
            database_name,
            original_file_name: file_name.ok_or_else(|| ApiError::bad_request("请选择上传文件"))?,
            note,
        },
        file_bytes.ok_or_else(|| ApiError::bad_request("请选择上传文件"))?,
    )
    .await?;

Ok(Json(run))
```

- [ ] **Step 6: 运行 API 测试**

新增或更新测试覆盖：

- 未认证拒绝上传。
- 缺少文件返回 400。
- 缺少目标返回 400。

Run:

```bash
cargo test api
```

Expected: PASS。

---

## Task 6: 前端类型和上传 API

**Files:**
- Modify: `apps/web/src/types/api.ts`
- Create: `apps/web/src/shared/api/manualUploads.ts`

- [ ] **Step 1: 扩展 `BackupRun`**

```ts
export type RunType = "scheduled" | "manualUpload";

export type BackupRun = {
  id: string;
  backupJobId: string;
  runType: RunType;
  status: RunStatus;
  stage: string;
  startedAt: string;
  finishedAt: string | null;
  durationMs: number | null;
  rawFileName: string | null;
  archiveFileName: string | null;
  fileSize: number | null;
  checksum: string | null;
  remotePath: string | null;
  fileDeleted: boolean;
  fileDeletedAt: string | null;
  errorMessage: string | null;
  createdAt: string;
};
```

- [ ] **Step 2: 新增上传 API**

```ts
import type { BackupRun } from "@/types/api";
import { ApiError } from "@/api/client";

export async function uploadManualBackup(token: string, data: FormData): Promise<BackupRun> {
  const response = await fetch("/api/manual-uploads", {
    method: "POST",
    headers: {
      Authorization: `Bearer ${token}`,
    },
    body: data,
  });

  if (!response.ok) {
    const payload = await response.json().catch(() => null);
    throw new ApiError(response.status, payload?.message || "手动上传失败");
  }

  return response.json();
}
```

---

## Task 7: 前端手动上传页面

**Files:**
- Create: `apps/web/src/features/manual-uploads/ManualUploadsPanel.tsx`
- Modify: `apps/web/src/App.tsx`
- Modify: `apps/web/src/app/navigation.tsx`

- [ ] **Step 1: 创建页面组件**

组件 props：

```ts
export function ManualUploadsPanel({
  targets,
  isSubmitting,
  onSubmit,
}: {
  targets: BackupTarget[];
  isSubmitting: boolean;
  onSubmit: (event: FormEvent<HTMLFormElement>) => Promise<boolean>;
})
```

表单字段：

- `backupTargetId`
- `sourceLabel`
- `databaseType`
- `databaseName`
- `note`
- `file`

- [ ] **Step 2: 空目标状态**

没有备份目标时显示：

```tsx
<EmptyState title="请先创建备份目标" description="手动上传需要选择一个远端备份目标。" />
```

- [ ] **Step 3: App 中增加 tab**

`TabKey` 增加：

```ts
type TabKey = "dashboard" | "sources" | "targets" | "jobs" | "manualUploads" | "runs";
```

路由增加：

```ts
manualUploads: "/manual-uploads",
```

- [ ] **Step 4: 实现提交处理**

```ts
async function handleManualUpload(event: FormEvent<HTMLFormElement>) {
  event.preventDefault();
  if (!token) return false;
  setIsSubmitting(true);
  setError("");
  const form = new FormData(event.currentTarget);
  try {
    const run = await uploadManualBackup(token, form);
    setActiveRunId(run.id);
    setSelectedRunId(run.id);
    toast.success("手动上传已开始");
    await refresh();
    navigate({ to: "/runs", search: { runId: run.id } });
    return true;
  } catch (uploadError) {
    setError(errorMessage(uploadError));
    return false;
  } finally {
    setIsSubmitting(false);
  }
}
```

- [ ] **Step 5: 导航增加“手动上传”**

在 `navigation.tsx` 增加：

```ts
{ key: "manualUploads", label: "手动上传", path: "/manual-uploads", icon: UploadCloud }
```

---

## Task 8: 执行历史展示 run 类型

**Files:**
- Modify: `apps/web/src/features/runs/RunsPanel.tsx`
- Modify: `apps/web/src/shared/formatters/run.ts`

- [ ] **Step 1: 增加阶段文案**

```ts
const stageLabels: Record<string, string> = {
  prepare: "准备",
  receive_file: "接收文件",
  dump: "导出",
  compress: "压缩",
  checksum: "校验",
  manifest: "生成清单",
  upload: "上传远端",
  verify_remote: "验证远端",
  local_cleanup: "清理本地",
  done: "完成",
};
```

- [ ] **Step 2: 增加类型筛选**

增加状态：

```ts
const [filterRunType, setFilterRunType] = useState<string>("all");
```

过滤逻辑：

```ts
if (filterRunType !== "all" && run.runType !== filterRunType) return false;
```

- [ ] **Step 3: 行内展示类型 badge**

在 run row 中增加：

```tsx
<Badge variant="secondary">
  {run.runType === "manualUpload" ? "手动上传" : "自动备份"}
</Badge>
```

- [ ] **Step 4: 分组兼容手动上传**

手动上传不按 `backupJobId` 分组，统一归入：

```ts
const key = run.runType === "manualUpload" ? "manualUpload" : run.backupJobId;
```

分组名：

```ts
if (group.key === "manualUpload") return "手动上传";
```

---

## Task 9: 文档更新

**Files:**
- Modify: `docs/02-architecture/数据库备份管理台架构设计.md`
- Modify: `docs/06-operations/部署快速开始.md`

- [ ] **Step 1: 架构文档增加手动上传链路**

写明：

```text
当数据库服务器无法被管理台访问时，用户可以上传已有数据库备份包。管理台只负责保存上传文件、计算校验值、生成 manifest、上传到备份目标并记录运行历史，不负责导出数据库。
```

- [ ] **Step 2: 运维文档增加路径规则**

写明：

```text
新自动备份远端路径包含 scheduled/ 前缀；手动上传远端路径包含 manual/ 前缀。旧版本生成的远端路径不会自动迁移。
```

- [ ] **Step 3: 增加配置说明**

写明：

```text
MAX_MANUAL_UPLOAD_BYTES=2147483648
MANUAL_UPLOAD_ALLOWED_EXTENSIONS=gz,zip,tar.gz,sql.gz,dump,bak,bacpac
```

---

## Task 10: 验证

**Files:**
- All changed files

- [ ] **Step 1: 后端格式化**

Run:

```bash
cargo fmt
```

Expected: command exits 0。

- [ ] **Step 2: 后端测试**

Run:

```bash
cargo test
```

Expected: all tests pass。

- [ ] **Step 3: 后端 lint**

Run:

```bash
cargo clippy --all-targets --all-features
```

Expected: no warnings or errors。

- [ ] **Step 4: 前端构建**

Run:

```bash
pnpm -C apps/web build
```

Expected: build succeeds。

- [ ] **Step 5: 手动浏览器验证**

启动服务：

```bash
cargo run -p backup-manager
```

在浏览器验证：

- 登录后能看到“手动上传”入口。
- 没有备份目标时提示先创建目标。
- 上传允许扩展名的文件后跳转执行历史。
- 执行历史显示“手动上传”类型。
- 手动上传日志出现 `receive_file`、`checksum`、`manifest`、`upload`、`verify_remote`。
- 新自动备份远端路径包含 `scheduled/`。
- 新手动上传远端路径包含 `manual/`。
- 成功上传后的文件可以下载。
- 成功上传后的文件可以删除。
- 上传非法扩展名返回错误。
- 上传超过大小限制的文件返回错误。

